# Plugin local configuration (user-specific settings)
.claude/*.local.md

# Runtime session state (should not be committed)
state/session.md
state/discovery-log.md

# User project state files (always-on orchestration)
.claude/prompt-orchestrator/
# Ignore JSON files except plugin.json and hooks.json which are tracked
!plugin.json
!hooks.json
*.json

# Hook runtime files
hooks/__pycache__/
*.pyc

# IDE and OS files
.DS_Store
.vscode/
.idea/
*.swp
*.swo
*~

# Dependencies (if any in future)
node_modules/
__pycache__/
*.pyc
